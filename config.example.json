{
  "_comment": "P Logo Updater Configuration File",

  "plex_url": "http://YOUR_SERVER_URL:32400",
  "plex_token": "YOUR_PLEX_TOKEN",

  "fanart_api_key": "YOUR_FANART_API_KEY",
  "fanart_key_type": "free",

  "connection_timeout": 60,
  "request_timeout": 10,
  "max_workers": 5,

  "enable_database": true,
  "database_path": "logo_history.db",

  "enable_backup": false,
  "backup_dir": "backups",

  "webhook_url": "",

  "_documentation": {
    "plex_url": "Full URL to your Plex server (e.g., http://192.168.1.100:32400)",
    "plex_token": "Your Plex authentication token - https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/",
    "fanart_api_key": "Your fanart.tv API key - Get one at https://fanart.tv/get-an-api-key/",
    "fanart_key_type": "Type of fanart.tv key: 'free' (1s delay) or 'paid' (0.5s delay)",
    "connection_timeout": "Plex connection timeout in seconds",
    "request_timeout": "HTTP request timeout in seconds",
    "max_workers": "Number of parallel workers for processing (1-10)",
    "enable_database": "Enable SQLite database for tracking processed items",
    "database_path": "Path to SQLite database file",
    "enable_backup": "Backup existing logos before replacing",
    "backup_dir": "Directory for logo backups",
    "webhook_url": "Optional webhook URL for notifications (Discord, Slack, etc.)"
  }
}
